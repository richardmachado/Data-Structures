# runtime complexity

commands = ['n', 's', 'w', 'e']

selection = input()

# in the worst case, we'd have to iterate over every element in the list tor find what we're looking for
# this is a linear operation

if selection in commands:
    # this is a valid command
    # perform the commond


#complexity classes

    # constant time - the effeciency of the code does not depend on the size of the input
    # constat runtime doesn't grow at all as the size increases
commands[3]

    # linear time - the effeciency of the code does depend on the size of the input
    # Linear runtime grows 1 to 1 as the size increases
for command in commands: # the size of the input has direct bearing on the effeciency
    print(command)

    for _ in commands:
        # O(n) * O(n) ~ O(n * n) == O(n^2)
        #Big O allows us to drop constants 
    # Constant < Linear
    # what's being compared is how quickly the effeciency grows as a result of the input size 

# Big O() Notation
    #Contant value O(1)
    #Linear time O(n)
